package app.lucene;

import java.util.*;

public class MergeSort {
    public static void mergeSort(int[] list, int len) {
        if(len < 2) {
            return;
        }
        int mid = len / 2;
        int[] left = list[mid];
    }

    public static void merge() {

    }
}
